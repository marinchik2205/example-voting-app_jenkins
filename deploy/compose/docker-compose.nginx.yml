services:
  nginx:
    image: nginx:alpine
    container_name: voting-nginx
    ports:
      - "8080:80"
    volumes:
      - ../../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped  